{
  plugins = {
    comment-nvim = {
      enable = true;

      # opleader = {line = "<C-b>";};
      # toggler = {line = "<C-b>";};
    };

    todo-comments = {
      enable = true;
      signs = false;
    };
  };
}
